services:
  alpine:
    image: alpine
    container_name: lab_alpine
    networks:
      - my-network
    # command: sh -c "apk add --no-cache iproute2 && sleep infinity"
    command: "sleep infinity"

networks:
  my-network:
    name: my-custom-net